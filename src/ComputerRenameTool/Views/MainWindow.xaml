<Window x:Class="ComputerRenameTool.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ComputerRenameTool.ViewModels"
        xmlns:views="clr-namespace:ComputerRenameTool.Views.UserControls"
        Title="Windows 机器名修改工具"
        Height="720" Width="720"
        MinHeight="640" MinWidth="640"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgPrimaryBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="{StaticResource UiFontFamily}"
        Loaded="MainWindow_OnLoaded">
    <Window.Resources>
        <vm:MainViewModel x:Key="MainVM" />
    </Window.Resources>

    <Window.DataContext>
        <Binding Source="{StaticResource MainVM}" />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Accent gradient bar (top) -->
        <Border Grid.Row="0" Height="2" CornerRadius="1" Margin="0,0,0,16">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#00D9FF" Offset="0" />
                    <GradientStop Color="#7C3AED" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Header -->
        <TextBlock Grid.Row="1"
                   Text="Windows 机器名修改工具"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   Margin="0,0,0,16" />

        <!-- 1. Rename (primary feature, top) -->
        <views:RenameSection Grid.Row="2" Margin="0,0,0,12" DataContext="{Binding Rename}" />

        <!-- 2. Hardware report (summary + collapsible detail) -->
        <ScrollViewer Grid.Row="3" Margin="0,0,0,12"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <views:HardwareReportSection DataContext="{Binding Hardware}" />
        </ScrollViewer>

        <!-- Spacer -->
        <Grid Grid.Row="4" />

        <!-- Footer hint + status bar -->
        <Border Grid.Row="5"
                Background="{StaticResource BgSecondaryBrush}"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="修改完成后需要重启电脑才能生效。"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" />
                <TextBlock Grid.Column="1"
                           Text="{Binding StatusMessage}"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource AccentCyanBrush}"
                           FontWeight="SemiBold" />
            </Grid>
        </Border>
    </Grid>
</Window>
